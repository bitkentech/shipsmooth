<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.bitken</groupId>
        <artifactId>shipsmooth</artifactId>
        <version>0.3.14</version>
    </parent>

    <artifactId>cli</artifactId>

    <properties>
        <picocli.version>4.7.5</picocli.version>
        <junit.jupiter.version>5.10.2</junit.jupiter.version>
        <jlink.exec.home>/opt/installers/jdk-semeru/jdk-25.0.2+10</jlink.exec.home>
        <jlink.jmods.home>/opt/installers/jdk-semeru/jdk-25.0.2+10</jlink.jmods.home>
        <jlink.jmods.darwin-x64>/opt/installers/jdk-semeru-mac-x64/Contents/Home</jlink.jmods.darwin-x64>
        <jlink.jmods.darwin-arm64>/opt/installers/jdk-semeru-mac-arm64/Contents/Home</jlink.jmods.darwin-arm64>
        <jlink.jmods.windows-x64>/opt/installers/jdk-semeru-win-x64/jdk-25.0.2+10</jlink.jmods.windows-x64>
        <jlink.jre.home>/opt/installers/jre-semeru/jdk-25.0.2+10-jre</jlink.jre.home>
        <jakarta.xml.bind-api.version>4.0.2</jakarta.xml.bind-api.version>
        <jaxb-runtime.version>4.0.5</jaxb-runtime.version>
        <jackson-databind.version>2.17.2</jackson-databind.version>
        <dagger.version>2.59.2</dagger.version>
        <jakarta.inject-api.version>2.0.1</jakarta.inject-api.version>
        <maven-compiler-plugin.version>3.12.1</maven-compiler-plugin.version>
        <jacoco-maven-plugin.version>0.8.11</jacoco-maven-plugin.version>
        <maven-antrun-plugin.version>3.1.0</maven-antrun-plugin.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>io.bitken</groupId>
            <artifactId>core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>info.picocli</groupId>
            <artifactId>picocli</artifactId>
            <version>${picocli.version}</version>
        </dependency>

        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson-databind.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
            <version>${jackson-databind.version}</version>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.jupiter.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!--
              Run tests on the classpath, not the module path. Under -Pjlink the core dependency
              jar is shaded with dagger.*; on the module path that collides with the real dagger
              module (JPMS split-package ResolutionException in the test fork). Tests need no
              module-layer behaviour, so classpath execution is both correct and conflict-free.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <useModulePath>false</useModulePath>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
          DE-RISK SCOPE (plan 68, Task 1): linux jlink image only, proving the two-module
          (io.bitken.ss.core + io.bitken.ss.cli) graph links and runs the version command.
          HARDEN phase will restore the 5 cross-platform builds, SCC launcher, smoke tests,
          and module-info re-injection from the original app/pom.xml jlink profile.
        -->
        <profile>
            <id>jlink</id>
            <properties>
                <!-- core JAR taken from its reactor target dir (no install needed under -am) -->
                <core.jar>${project.basedir}/../core/target/core-${project.version}.jar</core.jar>
                <jlink.runtime.module.path>${project.build.directory}/${project.artifactId}-${project.version}.jar:${core.jar}:${settings.localRepository}/info/picocli/picocli/${picocli.version}/picocli-${picocli.version}.jar:${settings.localRepository}/jakarta/xml/bind/jakarta.xml.bind-api/${jakarta.xml.bind-api.version}/jakarta.xml.bind-api-${jakarta.xml.bind-api.version}.jar:${settings.localRepository}/jakarta/activation/jakarta.activation-api/2.1.3/jakarta.activation-api-2.1.3.jar:${settings.localRepository}/org/glassfish/jaxb/jaxb-runtime/${jaxb-runtime.version}/jaxb-runtime-${jaxb-runtime.version}.jar:${settings.localRepository}/org/glassfish/jaxb/jaxb-core/${jaxb-runtime.version}/jaxb-core-${jaxb-runtime.version}.jar:${settings.localRepository}/org/eclipse/angus/angus-activation/2.0.2/angus-activation-2.0.2.jar:${settings.localRepository}/org/glassfish/jaxb/txw2/${jaxb-runtime.version}/txw2-${jaxb-runtime.version}.jar:${settings.localRepository}/com/sun/istack/istack-commons-runtime/4.1.2/istack-commons-runtime-4.1.2.jar:${settings.localRepository}/com/fasterxml/jackson/core/jackson-databind/${jackson-databind.version}/jackson-databind-${jackson-databind.version}.jar:${settings.localRepository}/com/fasterxml/jackson/core/jackson-core/${jackson-databind.version}/jackson-core-${jackson-databind.version}.jar:${settings.localRepository}/com/fasterxml/jackson/core/jackson-annotations/${jackson-databind.version}/jackson-annotations-${jackson-databind.version}.jar:${settings.localRepository}/com/fasterxml/jackson/datatype/jackson-datatype-jsr310/${jackson-databind.version}/jackson-datatype-jsr310-${jackson-databind.version}.jar:${settings.localRepository}/jakarta/inject/jakarta.inject-api/${jakarta.inject-api.version}/jakarta.inject-api-${jakarta.inject-api.version}.jar</jlink.runtime.module.path>
                <jlink.module.path>${jlink.runtime.module.path}:${jlink.jmods.home}/jmods</jlink.module.path>
            </properties>
            <build>
                <plugins>
                    <!--
                      Dagger is shaded into the CORE jar (see core/pom.xml jlink profile), because
                      DaggerAppComponents is generated into core and references dagger.internal.*
                      at runtime. core must not read cli, so the shaded classes must live in core.
                      The cli jar carries no dagger; here we only link and smoke-test the image.
                    -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>${maven-antrun-plugin.version}</version>
                        <executions>
                            <execution>
                                <id>jlink-version-check</id>
                                <phase>validate</phase>
                                <goals><goal>run</goal></goals>
                                <configuration>
                                    <target>
                                        <exec executable="${jlink.exec.home}/bin/jlink" failonerror="true">
                                            <arg value="--version"/>
                                        </exec>
                                    </target>
                                </configuration>
                            </execution>
                            <execution>
                                <id>jlink-build-image</id>
                                <phase>package</phase>
                                <goals><goal>run</goal></goals>
                                <configuration>
                                    <target>
                                        <delete dir="${project.build.directory}/jlink-image"/>
                                        <exec executable="${jlink.exec.home}/bin/jlink" failonerror="true">
                                            <arg value="--module-path"/>
                                            <arg value="${jlink.module.path}"/>
                                            <arg value="--add-modules"/>
                                            <arg value="io.bitken.ss.cli,openj9.sharedclasses"/>
                                            <arg value="--launcher"/>
                                            <arg value="shipsmooth=io.bitken.ss.cli/io.bitken.ss.cli.Shipsmooth"/>
                                            <arg value="--no-header-files"/>
                                            <arg value="--no-man-pages"/>
                                            <arg value="--compress"/>
                                            <arg value="zip-9"/>
                                            <arg value="--output"/>
                                            <arg value="${project.build.directory}/jlink-image"/>
                                        </exec>
                                    </target>
                                </configuration>
                            </execution>
                            <!-- Copy linux image to platform-named dir for PublishRelease -->
                            <execution>
                                <id>jlink-copy-linux-x64</id>
                                <phase>package</phase>
                                <goals><goal>run</goal></goals>
                                <configuration>
                                    <target>
                                        <delete dir="${project.build.directory}/jlink-image-linux-x64"/>
                                        <copy todir="${project.build.directory}/jlink-image-linux-x64">
                                            <fileset dir="${project.build.directory}/jlink-image"/>
                                        </copy>
                                    </target>
                                </configuration>
                            </execution>
                            <!-- Cross-compile macOS x64 image using Linux jlink + Mac jmods -->
                            <execution>
                                <id>jlink-build-darwin-x64</id>
                                <phase>package</phase>
                                <goals><goal>run</goal></goals>
                                <configuration>
                                    <target>
                                        <delete dir="${project.build.directory}/jlink-image-darwin-x64"/>
                                        <exec executable="${jlink.exec.home}/bin/jlink" failonerror="true">
                                            <arg value="--module-path"/>
                                            <arg value="${jlink.runtime.module.path}:${jlink.jmods.darwin-x64}/jmods"/>
                                            <arg value="--add-modules"/>
                                            <arg value="io.bitken.ss.cli,openj9.sharedclasses"/>
                                            <arg value="--launcher"/>
                                            <arg value="shipsmooth=io.bitken.ss.cli/io.bitken.ss.cli.Shipsmooth"/>
                                            <arg value="--no-header-files"/>
                                            <arg value="--no-man-pages"/>
                                            <arg value="--compress"/>
                                            <arg value="zip-9"/>
                                            <arg value="--output"/>
                                            <arg value="${project.build.directory}/jlink-image-darwin-x64"/>
                                        </exec>
                                    </target>
                                </configuration>
                            </execution>
                            <!-- Cross-compile macOS arm64 image using Linux jlink + ARM64 Mac jmods -->
                            <execution>
                                <id>jlink-build-darwin-arm64</id>
                                <phase>package</phase>
                                <goals><goal>run</goal></goals>
                                <configuration>
                                    <target>
                                        <delete dir="${project.build.directory}/jlink-image-darwin-arm64"/>
                                        <exec executable="${jlink.exec.home}/bin/jlink" failonerror="true">
                                            <arg value="--module-path"/>
                                            <arg value="${jlink.runtime.module.path}:${jlink.jmods.darwin-arm64}/jmods"/>
                                            <arg value="--add-modules"/>
                                            <arg value="io.bitken.ss.cli,openj9.sharedclasses"/>
                                            <arg value="--launcher"/>
                                            <arg value="shipsmooth=io.bitken.ss.cli/io.bitken.ss.cli.Shipsmooth"/>
                                            <arg value="--no-header-files"/>
                                            <arg value="--no-man-pages"/>
                                            <arg value="--compress"/>
                                            <arg value="zip-9"/>
                                            <arg value="--output"/>
                                            <arg value="${project.build.directory}/jlink-image-darwin-arm64"/>
                                        </exec>
                                    </target>
                                </configuration>
                            </execution>
                            <!-- Cross-compile Windows x64 image using Linux jlink + Windows jmods -->
                            <execution>
                                <id>jlink-build-windows-x64</id>
                                <phase>package</phase>
                                <goals><goal>run</goal></goals>
                                <configuration>
                                    <target>
                                        <delete dir="${project.build.directory}/jlink-image-windows-x64"/>
                                        <exec executable="${jlink.exec.home}/bin/jlink" failonerror="true">
                                            <arg value="--module-path"/>
                                            <arg value="${jlink.runtime.module.path}:${jlink.jmods.windows-x64}/jmods"/>
                                            <arg value="--add-modules"/>
                                            <arg value="io.bitken.ss.cli,openj9.sharedclasses"/>
                                            <arg value="--launcher"/>
                                            <arg value="shipsmooth=io.bitken.ss.cli/io.bitken.ss.cli.Shipsmooth"/>
                                            <arg value="--no-header-files"/>
                                            <arg value="--no-man-pages"/>
                                            <arg value="--compress"/>
                                            <arg value="zip-9"/>
                                            <arg value="--output"/>
                                            <arg value="${project.build.directory}/jlink-image-windows-x64"/>
                                        </exec>
                                    </target>
                                </configuration>
                            </execution>
                            <!-- Write full-JDK launcher with OpenJ9 SCC + quickstart -->
                            <execution>
                                <id>jlink-write-scc-launcher</id>
                                <phase>package</phase>
                                <goals><goal>run</goal></goals>
                                <configuration>
                                    <!-- TODO: Make scc-launcher cross-platform -->
                                    <target>
                                        <mkdir dir="${project.build.directory}/scc-launcher"/>
                                        <echo file="${project.build.directory}/scc-launcher/shipsmooth">#!/bin/sh
SCC_DIR="${project.build.directory}/scc"
mkdir -p "$SCC_DIR"
exec ${jlink.jre.home}/bin/java \
  -Xquickstart \
  -Xshareclasses:name=shipsmooth_v${project.version},cacheDir="$SCC_DIR",nonfatal \
  --module-path ${jlink.runtime.module.path} \
  -m io.bitken.ss.cli/io.bitken.ss.cli.Shipsmooth "$@"
</echo>
                                        <chmod file="${project.build.directory}/scc-launcher/shipsmooth" perm="755"/>
                                    </target>
                                </configuration>
                            </execution>
                            <!-- Smoke tests in verify phase -->
                            <execution>
                                <id>jlink-smoke-help</id>
                                <phase>verify</phase>
                                <goals><goal>run</goal></goals>
                                <configuration>
                                    <target>
                                        <exec executable="${project.build.directory}/scc-launcher/shipsmooth"
                                              failonerror="true">
                                            <arg value="--help"/>
                                        </exec>
                                    </target>
                                </configuration>
                            </execution>
                            <execution>
                                <id>jlink-smoke-show</id>
                                <phase>verify</phase>
                                <goals><goal>run</goal></goals>
                                <configuration>
                                    <target>
                                        <exec executable="${project.build.directory}/scc-launcher/shipsmooth"
                                              failonerror="true"
                                              dir="${project.basedir}/..">
                                            <arg value="plan"/>
                                            <arg value="show"/>
                                            <arg value="--plan"/>
                                            <arg value="27"/>
                                        </exec>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
