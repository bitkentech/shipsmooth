@param io.bitken.shipsmooth.resources.PluginModel model

2. Perform an `invoke_agent` tool call: `agent_name: generalist`, prompt = `payload`. The resolver prompt includes the absolute worktree path and instructs the agent to use absolute paths for all file operations.
