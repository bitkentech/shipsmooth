@param io.bitken.ss.resources.PluginModel model

**Not applicable on Codex (this cut).** The background `integrate` step coordinates the merge of parallel subagent branches. Since parallel dispatch is not yet supported on the Codex host, tasks are executed sequentially in the main context and committed in order — there is no integration/resolver cycle to run in the background. Skip this section; proceed once all tasks are committed.
