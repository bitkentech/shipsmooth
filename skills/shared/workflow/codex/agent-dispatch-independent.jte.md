@param io.bitken.ss.resources.PluginModel model

3. Implement the task yourself in $WORKTREE — Codex parallel subagent dispatch is not yet supported, so the Lead Agent runs the task directly in the main context (see Worker Instruction Block below)
