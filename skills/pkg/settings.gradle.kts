rootProject.name = "skills-pkg"
// Phase 0 of the Maven->Gradle migration trial (plan-71): skills:pkg only.
// No other modules are included — core/cli/packaging are out of scope until/unless
// this trial passes. See docs/proposals/build-migrate.md.
