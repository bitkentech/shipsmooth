# Gradle trial (plan-71, Phase 0) transient output — never commit these.
.gradle/
build/
buildSrc/.gradle/
buildSrc/build/
# JTE plugin's default precompile target dir (unused now — we use generate mode
# into build/generated-sources — but guard against it being recreated).
jte-classes/
