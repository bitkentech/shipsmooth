# plan-100 — update readme with opencode install steps

## Context

Feature (in the user's words): update readme with opencode install steps

Add an **OpenCode** installation section to the README, alongside the existing Claude Code, Gemini CLI, and Codex CLI sections.

## Tasks

### Task 1: Add OpenCode installation section to README [Low]

Add a new `### OpenCode` subsection under `## Installation` in `README.md`, after the existing Codex CLI section. Content:

- JSON config block for `opencode.json` with the plugin entry
- Restart instruction
- Workflow load instruction (`/shipsmooth:start`)

No other changes to README. No code, no tests — documentation-only.

## Backlog Issue

_None — documentation-only improvement._
